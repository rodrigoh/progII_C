#include<stdio.h>

int main(){
  // int numero;
  // float altura;
  // char letra;
  // printf("Digite um valor inteiro: ");
  // scanf("%d",&numero);
  // printf("O valor digitado foi %d\n",numero);
  // printf("Digite sua altura em metros: ");
  // scanf("%f",&altura);
  // printf("Sua altura e %.2f metros\n",altura);
  // printf("Digite a primeira letra do seu nome: ");
  // //Limpa o buffer do teclado
  // setbuf(stdin,NULL);
  // scanf("%c",&letra);
  // printf("A primeira letra do seu nome e: %c\n",letra);
  // //Leitura de uma letra do teclado.
  // letra = getchar();
  // printf("%c\n",letra);
  int a = 1;
  int b = 2;
  float c = a/(float)b;
  printf("A divisao vale %.2f%c\n",c,37);
  printf("A divisao vale %.2f%%\n",c);
  return 0;
}